---

docker_dependencies:
  pip_packages:
    - docker
  packages:
    - gnupg2
    - apt-transport-https
    - ca-certificates
    - software-properties-common

docker_packages:
  - containerd.io
  - docker-ce
